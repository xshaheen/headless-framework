---
pr: 194
branch: worktree-xshaheen/messaging-circuit-breaker-and-retry-backpressure
reviewers: [strict-dotnet-reviewer, pragmatic-dotnet-reviewer, code-simplicity-reviewer, security-sentinel, performance-oracle, agent-native-reviewer, learnings-researcher]
findings:
  p1_critical: 4
  p2_important: 12
  p3_nice_to_have: 8
  total: 24
timestamp: 2026-03-21T00:00:00Z
pass: 2
---

# Code Review Summary — Pass 2

Second-pass review after 27 todos from Pass 1 were fully resolved. Core state machine logic, escalation, probe semantics, OTel cardinality guards, and metrics integration are all sound. This pass found remaining correctness bugs, security gaps, and agent-native API gaps.

## Reviewers Used
- `strict-dotnet-reviewer` — correctness, thread safety, C# idioms
- `pragmatic-dotnet-reviewer` — operational traps, DX, usability
- `code-simplicity-reviewer` — dead code, over-engineering, test flakes
- `security-sentinel` — log injection, input validation, DoS vectors
- `performance-oracle` — hot-path allocations, lock contention
- `agent-native-reviewer` — programmatic API completeness
- `learnings-researcher` — cross-reference docs/solutions/

## Key Findings

### P1 — Correctness Bugs (Blocks Merge)
- **SNS double-check wrong field**: `_sqsClient == null` guard in SNS init block → SNS client leak (todo 106)
- **ConsecutiveFailures increments while Open**: default arm too broad, counter grows unbounded (todo 107)
- **Kafka no pause gate**: broker-level pause wiped on rebalance → nack storm during open window (todo 108)
- **TCS write-after-dispose in 4 transports**: PauseAsync can replace gate after DisposeAsync → infinite block on shutdown (todo 109)

### P2 — Important
- Dispose() no-op on cancellation (118), log injection in groupName/Reason (119,120), misleading close log (121), no per-consumer options validation (122), dead double-cap code (123), O(N) Count on hot path (124), GetAllStates async footgun (125), no KnownGroups enumeration (126), no ResetBackpressureAsync (127), Timer inside lock (128), RedisConsumerClient missing sealed (105)

### P3 — Nice-to-Have
- Unreachable ContinueWith (110), Task.Delay test flakes (111), RabbitMQ resume-after-dispose (112), EscalationLevel unbounded (113), SubscribeExecutor unsanitized log (114), _GetOpenDuration +∞ clarity (115), KafkaConsumerClient public visibility (116), CircuitBreakerSnapshot type (117)

## References
- Todos: `docs/todos/*-pending-*.md`
- PR: https://github.com/xshaheen/headless-framework/pull/194
- Related solutions: `docs/solutions/concurrency/circuit-breaker-transport-thread-safety-patterns.md`
