# CI restores NuGet packages into the workspace (.nuget/packages via NUGET_PACKAGES in ci.yml);
# third-party package content must never gate formatting.
.nuget/
.worktrees/
artifacts/

# CSharpier's XML check disagrees on these hand-maintained files between local (macOS) and CI (Linux)
# runs of the same tool version; they are build infrastructure, not authored code, so they are
# excluded from the gate (the readable hand-formatted attribute layout is kept intentionally).
nuget.config
eng/DashboardSpa.targets
